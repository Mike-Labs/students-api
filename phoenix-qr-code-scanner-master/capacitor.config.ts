import type { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'com.phoenixdimas.android.kinderhaus',
  appName: 'kinderhaus',
  webDir: 'www'
};

export default config;
